apiVersion: v1
kind: ResourceQuota
metadata:
  name: cohort-quota
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "15"
    persistentvolumeclaims: "3"
    services.loadbalancers: "0"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: cohort-defaults
spec:
  limits:
    - type: Container
      default:
        cpu: 200m
        memory: 256Mi
      defaultRequest:
        cpu: 50m
        memory: 64Mi
