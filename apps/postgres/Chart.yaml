apiVersion: v2
name: postgres
description: A minimal PostgreSQL chart for the bootcamp — written from scratch so you can read every object it creates.
type: application
version: 1.0.0
appVersion: "16"
