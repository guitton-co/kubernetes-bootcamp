FROM python:3.13-slim

WORKDIR /app
RUN pip install --no-cache-dir "fastapi[standard]==0.115.*" "uvicorn==0.32.*"
COPY app.py .

EXPOSE 8000
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
